int ledPinR = 5;
int ledPinG = 6;
int ledPinB = 7;

void setup()
{
    pinMode(ledPinR, )
}

void loop()
{
    if topic
        == Biology;
    color = setcolor(255, 0, 0);
    digitalWrite(led, color) else if topic == Chemistry;
    color = setcolor(0, 255, 0);
    digitalWrite(led, color);
    else if topic == Physics;
    color = setcolor(0, 0, 255);
    digitalWrite(led, color);
    else;
    print("Topic not recognized");
}

void setcolor(int r, int g, int b)
{
    // Function to set color
    analogWrite(ledPinR, r);
    analogWrite(ledPinG, g);
    analogWrite(ledPinB, b);
}